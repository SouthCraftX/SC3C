#include <string.h>

int argvpro( struct _PNGData*  png , struct _ResPackArchive* respack ,
             const int argc , const char* argv[] )
{
    for( int now_argc = 1, now_argc < argc , ++now_argc ){
        if( !strcmp( argv[now_argc] , "-i" )){
            respack->
        }
    }
}